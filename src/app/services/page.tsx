import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Services",
  description:
    "Explore our custom business automation services — from efficiency analysis to fully engineered automation systems.",
};

export default function ServicesPage() {
  return (
    <div>
      {/* TODO: Service hero, detail sections, CTA */}
    </div>
  );
}
